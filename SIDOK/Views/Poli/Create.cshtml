@model SIDOK.Models.Poli

@{
    ViewData["Title"] = "Create";
}

<h1>Add</h1>

<h4>Poli</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" id="myForm" onsubmit="createForm(event)">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NamaPoli" class="control-label"></label>
                <input asp-for="NamaPoli" class="form-control" />
                <span asp-validation-for="NamaPoli" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LokasiPoli" class="control-label"></label>
                <input asp-for="LokasiPoli" class="form-control" />
                <span asp-validation-for="LokasiPoli" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
